{
  "name": "Python Hello World",
  "image": "python:3.9",
  "extensions": [],
  "runArgs": ["-it", "--rm", "--entrypoint=/bin/bash"],
  "postCreateCommand": "apt-get update && apt-get install -y python3 && python3 -c 'print(\"Hello, World!\")'"
}
